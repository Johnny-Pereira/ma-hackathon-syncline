import React from "react";
import "./forum.css";

interface ForumProps {
    // TODO: Add props here
}

export const Forum: React.FC<ForumProps> = (props) => {
    return (
        <div>
            <h1>Landing Page</h1>
        </div>
    );
};

export default Forum;